import 'package:flutter/material.dart';

class RewardList extends StatelessWidget {
  const RewardList({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      // TODO: J - Consider using Listview.builder and ListTile
    );
  }
}